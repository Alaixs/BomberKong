#ifndef DONKEYKONG_H
#define DONKEYKONG_H
#include "entity.h"

class DonkeyKong : public Entity
{
public:
    Donk(int posX, int posY);
    BomberGirl(Coordinate pos);

    void update();
    void draw(QPainter * painter);

    void collisionEvent(Entity * body);

    QRect getRect();

private:
    QPixmap sprite;
};

#endif // DONKEYKONG_H
